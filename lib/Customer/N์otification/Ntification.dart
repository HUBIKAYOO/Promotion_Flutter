import 'package:flutter/material.dart';

class Ntification extends StatelessWidget {
  const Ntification({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ไม่มีการเเจ้งเตือน"),),
    );
  }
}